<h1>{{title}}</h1>
{{#if books.length}}
<p><strong>Delete the following books before attempting to delete this author.</strong></p>
<div style="margin-left:20px;margin-top:20px">
    <h4>Books</h4>
    <dl>
        {{#each books}}
        <dt>
            <a href="{{this.url}}">{{this.title}}</a>
        </dt>
        <dd>
            {{this.summary}}
        </dd>
        {{/each}}
    </dl>
</div>
{{else}}
<p>Do you really want to delete this book instance?</p>
<form action="" method="POST">
    <div class="form-group">
        <input type="hidden" name="genreid" id="genreid" class="form-control"
        required="true" value="{{genre._id}}">
    </div>
    <button class="btn btn-danger" type="submit">Submit</button>
</form>
{{/if}}