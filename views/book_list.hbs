<h1>{{title}}</h1>

<ul>
    {{#if book_list}}
    {{#each book_list}}
<li><a href="{{this.url}}"><strong>{{this.title}}</strong></a>  - <em>{{this.author.name}}</em></li>
    {{/each}}
    {{else}}
    <li>There are no books.</li>
    {{/if}}
</ul>